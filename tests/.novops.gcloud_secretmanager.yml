# GCloud Secret Manager test
name: test-gcloud-secret-manager

environments:
  dev:
    variables:
    - name: SECRETMANAGER_VAR_STRING
      value:
        gcloud_secret:
          name: projects/pierre-sandbox-372512/secrets/TestSecret/versions/latest
  
    files:
    - name: SECRETMANAGER_VAR_FILE
      dest: /tmp/gcloud_SECRETMANAGER_VAR_FILE
      content:
        gcloud_secret:
          name: projects/pierre-sandbox-372512/secrets/TestSecret/versions/latest
          validate_crc32c: false

config:
  default:
    environment: dev
